<%= form_with model: @address do |form| %>
    <div class="field">
      <%= form.label :cep, 'CEP' %>
      <%= form.text_field :cep, required: true %>
    </div>

    <div class="field">
      <%= form.label :address, 'Endereço' %>
      <%= form.text_field :address, required: true %>
    </div>

    <div class="actions">
      <%= form.submit "Confirmar" %>
    </div>
<% end %>