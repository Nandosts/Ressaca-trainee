<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <%= stylesheet_link_tag 'product_form'%>

</head>
<body>

<div class="box">
  <%= form_with model: @product do |form| %>
      <input type="text" placeholder="Nome" class="field" <%= form.text_field :name, required: true %>

      <input type="number" placeholder="Valor" class="field" <%= form.number_field :value, min: 0, step: 0.01 %>

      <input type="number" placeholder="Volume" class="field" <%= form.number_field :volume, min: 0, step: 1 %>

      <input type="number" placeholder="Quantidade" class="field" <%= form.number_field :quantity, min: 0, step: 1 %>

      <select type="text" name="Tipo de Bebida" class="field" <%= form.collection_select :drink_type_id, @drink_types, :id, :name, {:prompt => "Tipo de Bebida"} %>

      <textarea type="text_area" placeholder="Descrição do Produto" class="field" <input type="text" placeholder="Nome" class="field" <%= form.text_area :description %>

      <input type="file" placeholder="Imagem" class="field" <%= form.file_field :photo %>
    
      <%= form.submit "Confirmar" %>
  <% end %>
</div>

</body>
</html>
